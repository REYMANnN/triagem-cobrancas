{
  "name": "triagem-cobrancas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/domain.js && node --check src/prompts.js && node --check src/ui.js",
    "deploy": "wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "GROQ_API_KEY": {
        "description": "Chave da API da Groq. Crie em https://console.groq.com/keys e mantenha como Secret."
      }
    }
  }
}
